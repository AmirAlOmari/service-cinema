{
  "extends": "./tsconfig.json",
  // Avoid
  // dist/src
  // dist/scripts
  "include": ["src/**/*"],
  // Do not use .ts prefix (case of "allowJs: true")
  "exclude": ["node_modules", "tests", "**/*.spec.*", "**/__mocks__"]
}
